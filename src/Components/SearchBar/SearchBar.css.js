const searchBarStyle = {
    inputZone:{
        display: 'flex',
        //flexDirection: window.innerWidth < 600 ? 'column' : 'row', 
        alignItems: 'center',
    }
}

export default searchBarStyle;
